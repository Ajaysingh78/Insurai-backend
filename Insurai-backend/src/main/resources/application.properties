# Server Configuration - PORT ko environment se read karein
server.port=${PORT:8080}

# Database - Railway/Render Database URL use karenge
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/insurai_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:AJAY78@mysql}

# JWT Secret - Environment variable se
jwt.secret=${JWT_SECRET:InsurAI-Super-Secret-Key-2024-Change-This-To-Something-Very-Long-And-Random-At-Least-256-Bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Mail Configuration
spring.mail.username=${MAIL_USERNAME:ajaygurjar78692@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Ajay@78692}

# Supabase
supabase.url=${SUPABASE_URL:https://dummy-url.supabase.co}
supabase.accessKey=${SUPABASE_ACCESS_KEY:dummy-access-key}
supabase.secretKey=${SUPABASE_SECRET_KEY:dummy-secret-key}

# Cohere API
cohere.api.key=${COHERE_API_KEY:dummy-api-key}